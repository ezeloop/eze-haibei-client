import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#91DDCF',
    },
    secondary: {
      main: '#F7F9F2',
    },
    warning: {
      main: '#F19ED2',
    }
  },
});

export default theme;
